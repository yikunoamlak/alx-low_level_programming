#include <stdio.h>
#include <stdlib.h>

/**
 * main - main entry
 * Description: prints all the numbers of base 16 in lowercase
 * Return: 0
 */
int main(void)
{
	int a;

	for (a = 48; a < 58; a++)
		putchar(i);
	for (a = 'a'; a <= 'f'; a++)
		putchar(i);
	putchar('\n');
	return (0);
}
